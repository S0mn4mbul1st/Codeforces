#include <bits/stdc++.h>

using namespace std;

void solve(){
    int a, b;

    cin >> a >> b;

    while(b --){

        if(a % 10 != 0)
            a --;
        else a /= 10;
    }
    cout << a << endl;
}

int main()
{
    solve();

    return 0;
}
